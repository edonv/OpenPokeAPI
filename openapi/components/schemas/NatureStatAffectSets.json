{
  "type": "object",
  "required": [
    "increase",
    "decrease"
  ],
  "properties": {
    "increase": {
      "description": "A list of natures and how they change the referenced stat.",
      "$ref": "./NamedAPIResourceArray.json"
    },
    "decrease": {
      "description": "A list of natures and how they change the referenced stat.",
      "$ref": "./NamedAPIResourceArray.json"
    }
  }
}
